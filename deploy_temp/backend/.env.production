# Same as env.template — fill in cPanel MySQL credentials before packaging, or edit .env on the server after upload.
# Do not commit real passwords to a public repo.

PORT=3000
DB_TYPE=mysql

DB_HOST=localhost
DB_NAME=your_cpanel_database_name
DB_USER=your_cpanel_database_user
DB_PASSWORD=your_database_password
